# Kubernetes Manifests

This directory will contain Kubernetes manifests for deploying Vecinita services.

## Planned Structure

```
k8s/
  namespaces/
  deployments/
    frontend.yaml
    proxy.yaml
    scraper-service.yaml
    model-service.yaml
    embedding-service.yaml
  services/
  configmaps/
  secrets/
  ingress/
```

## Getting Started

> Kubernetes manifests are not yet added. Use `infra/docker-compose.yml` for local development.
