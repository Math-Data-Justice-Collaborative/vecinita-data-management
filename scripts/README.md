# Scripts

Developer utility scripts for the Vecinita monorepo.

## Available Scripts

> Scripts will be added here as the project grows.

Planned:

- `install-all.sh` — install all packages and services in editable mode
- `lint-all.sh` — run linters across all packages and services
- `test-all.sh` — run tests across all packages and services
